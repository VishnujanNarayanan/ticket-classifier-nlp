# Runtime dependencies. Streamlit Community Cloud installs this file and only this
# file, so it must stay lean and contain everything the deployed app imports.
# Development tooling lives in requirements-dev.txt.
#
# PYTHON VERSION MATTERS HERE. scikit-learn is pinned to the exact version that
# pickled artifacts/*.joblib, and 1.3.2 ships wheels for cp38-cp312 only. On a newer
# interpreter pip falls back to building it from source and the Cython build fails.
# Streamlit Cloud defaults to the latest Python, so the app must be deployed with
# Python 3.11 selected in Advanced settings (see README -> Deploying).
scikit-learn==1.3.2
numpy==1.24.4
pandas==2.0.3
streamlit==1.39.0
nltk==3.9.1
joblib==1.4.2
openpyxl==3.1.5
