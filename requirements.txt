# Runtime dependencies. Streamlit Community Cloud installs this file and only this
# file, so it must stay lean and must contain everything the deployed app imports.
# Development tooling lives in requirements-dev.txt.
streamlit==1.39.0
scikit-learn==1.3.2
nltk==3.9.1
pandas==2.0.3
numpy==1.24.4
joblib==1.4.2
openpyxl==3.1.5
gradio==4.44.1
