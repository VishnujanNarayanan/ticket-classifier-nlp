# Runtime dependencies for the deployed app. Smaller than requirements.txt, which
# also pins the dev toolchain (ruff, pytest, jupyter, gradio).
streamlit==1.39.0
scikit-learn==1.3.2
nltk==3.9.1
pandas==2.0.3
numpy==1.24.4
joblib==1.4.2
