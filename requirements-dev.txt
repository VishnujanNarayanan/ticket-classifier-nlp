# Development and CI tooling. Installs the runtime set too.
# gradio is here rather than in requirements.txt because only the LOCAL app.py needs
# it; the deployed app is streamlit_app.py. Keeping it out of the runtime set makes
# the deploy install smaller and removes a dependency-resolution conflict with
# streamlit over shared packages (rich, click, typer).
-r requirements.txt
gradio==4.44.1
ruff==0.12.0
pytest==8.3.5
matplotlib==3.7.5
